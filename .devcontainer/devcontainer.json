{
  "name": "compliant-b2b-outreach-automation",
  "build": {"dockerfile": "../Dockerfile", "context": ".."},
  "forwardPorts": [8000],
  "postCreateCommand": "pip install -e '.[dev]' && outreach-bot init-db",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "charliermarsh.ruff"]
    }
  }
}
